# build image時，.env 檔案不應該被包含在image中，環境變數應該透過 Kubernetes Secret 或 ConfigMap 傳遞。

# MONGO_URI=mongodb://<username>:<password>@<hostname>:<port>/<database-name>
MONGO_URI=mongodb://127.0.0.1:33333/cnf

# API 伺服器運行的連接埠
PORT=3000